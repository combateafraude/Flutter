name: new_face_liveness_compatible
description: Flutter plugin cross-platform (iOS, Android) for Combate à Fraude's PassiveFaceLiveness (https://github.com/combateafraude).
version: 3.1.0
homepage: https://www.combateafraude.com/

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.caf.face_liveness
        pluginClass: FaceLivenessPlugin
      ios:
        pluginClass: PassiveFaceLivenessPlugin
