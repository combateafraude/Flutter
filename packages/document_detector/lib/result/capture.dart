class Capture {
  String? imagePath;
  String? imageUrl;
  String? label;
  double? quality;
  int? lensFacing;

  Capture(
      this.imagePath, this.imageUrl, this.label, this.quality, this.lensFacing);
}
